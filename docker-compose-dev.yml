version: "2"
services:
  topic-extract-dev:
    container_name: topic-extract-dev
    build:
      context: app/
      dockerfile: deployment/dev/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
      - ./models:/app/models
      - ./data:/app/data